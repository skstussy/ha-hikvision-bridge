{
  "config": {
    "step": {
      "user": {
        "title": "Add Hikvision recorder",
        "description": "Enter the recorder connection settings used by Home Assistant to reach the Hikvision NVR or DVR.\n\nUse the local IP address or DNS name reachable from Home Assistant. Leave Verify SSL disabled when the recorder uses a self-signed certificate. Use an HTTPS port only when HTTPS is enabled on the recorder.",
        "data": {
          "host": "Host or IP address",
          "port": "Port",
          "username": "Username",
          "password": "Password",
          "use_https": "Use HTTPS",
          "verify_ssl": "Verify SSL certificate"
        }
      },
      "reconfigure": {
        "title": "Reconfigure Hikvision recorder",
        "description": "Update the recorder connection settings without removing the integration entry.\n\nUse this when the recorder host, port, protocol, or credentials have changed.\n\nKeep the same recorder reachable from Home Assistant. Re-test credentials before saving. Enable Verify SSL only when Home Assistant trusts the recorder certificate.",
        "data": {
          "host": "Host or IP address",
          "port": "Port",
          "username": "Username",
          "password": "Password",
          "use_https": "Use HTTPS",
          "verify_ssl": "Verify SSL certificate"
        }
      }
    },
    "error": {
      "cannot_connect": "Unable to connect to the recorder or complete Hikvision digest authentication.",
      "already_configured": "Another entry already uses this host and port."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Debug options",
        "description": "Enable targeted debug categories for troubleshooting. Use a comma-separated list. Example: playback, isapi, websocket",
        "data": {
          "debug_enabled": "Enable debug logging",
          "debug_categories": "Debug categories"
        }
      }
    }
  }
}